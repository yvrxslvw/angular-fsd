export { generateSwagger } from './generate-swagger';
import { MappedPost, MappedRole, MapperUser, Post, Role, User } from './examples';

export const SwaggerExample = {
	User,
	MapperUser,
	Post,
	MappedPost,
	Role,
	MappedRole,
};
